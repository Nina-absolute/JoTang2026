### #1 安装 Git 环境

参考[Git 安装配置教程 124](https://www.runoob.com/git/git-install-setup.html)

在 Linux 平台上使用
```
apt-get install git
```

***==Git 的学习思路：==利用常见生活场景辅助理解和记忆 Git 相关命令的内涵。***
### #2 Git 必记知识

- 概念辨析
	工作区：我的编辑区
	暂存区：打包文件（类似于打包台）
	本地仓库：我的仓库（类似于快递箱）
	远程仓库：接收我文件的仓库（接受地）

交付流程be like：
修改好文件 → `add`( 把文件打包到暂存区 ) → `commit` （暂存区所有文件交到本地仓库去）→ `push` （交到远程仓库）

- 创建仓库
```
git init( 中文意思？ )
git clone + 网址链接
```

- 文件管理
关于 `mkdir`
`mkdir files` == 创建一个叫files的文件夹
`mkdir -p` 
(`-p` 是一个)

- `commit message`书写格式
```
<类型>(作用域): <简明描述>

<详细描述（可选，换一行写）>
``` 

### #3 ==**类型（Type）速查表==：**

| 类型          | 使用场景（必记）          | 你的任务对应                 |
| ----------- | ----------------- | ---------------------- |
| **`docs`**  | 只改文档（README、注释）   | **90% 的情况都用这个**        |
| **`feat`**  | 新增功能代码            | 你写了个函数                 |
| **`fix`**   | 修复 bug            | 你改了个报错                 |
| **`chore`** | 构建工具、配置文件改动       | 添加 `.gitignore` 或初始化仓库 |
| `style`     | 代码格式（空格、分号，不影响逻辑） | 调整缩进                   |

**每完成一个独立的小步骤，就提交一次** == 便于回退

`git commit -m "commit message"`

`git log --oneline`简洁方式查看 git 操作历史
`cat filename`查看文件内容
`git status`
`git add index`

|        命令         | 工作区 | 暂存区 |
| :---------------: | :-: | :-: |
| git reset --soft  | [x] | [x] |
| git reset --hard  | [ ] | [ ] |
| git reset --mixed | [x] | [ ] |
`git reflog`
`ls`
`HEAD`最新提交节点

- `git diff`
- 查看版本之间的差异内容
- 发生变更的文件
- 文件内容：用哈希算法显示一个40位哈希值
- 

- 默认比较：工作区和暂存区
- 比较工作区与版本库（本地仓库）：`git diff HEAD`
- 比较暂存区与版本库：`git diff --cached`
- 比较两个指定版本：`git diff commit-hash1 commit-hash2`
- 比较当前版本与前n个版本：`git diff HEAD~n HEAD `
- 比较两版本某个文件：`git diff HEAD~n HEAD filename`
- 
`vi filename` 

删除文件

### #4 查看文件

 - **`ls -ltr`** 是 **操作系统命令**，查看**硬盘磁盘**上的真实文件和文件夹。
	参数拆解：
	- `-l`：以长格式显示（权限、大小、修改时间）。
    - `-t`：按**修改时间**排序（最新的排最前面，但加了 `-r` 就反过来，最新排在最后面）。
    - `-r`：反转排序顺序。

- **`git ls-files`** 是 **Git 命令**，查看 **Git 仓库索引（暂存区/提交记录）** 中被跟踪的文件。

### 扩展点：`git ls-files` 的实用场景

默认情况下 `git ls-files` 只是列名字，但在实际工作中，通常配合参数解决特定问题：

| 场景                   | 命令组合                       | 说明                                                  |
| -------------------- | -------------------------- | --------------------------------------------------- |
| **查看所有被 Git 跟踪的文件**  | `git ls-files`             | 确认哪些代码被纳入了版本控制。                                     |
| **查看所有未被 Git 跟踪的文件** | `git ls-files -o`          | 列出你忘了 `add` 的新文件。（`-o`是`--others`=列出所有未被 Git 跟踪的文件） |
| **查看暂存区的文件**         | `git ls-files --stage`     | 显示文件在暂存区中的权限和 Hash 值。                               |
| **删除所有被跟踪的文件（危险）**   | `git ls-files \| xargs rm` | 会删掉硬盘上所有被 Git 管理的文件（留空仓库），新手慎用！                     |


法一：rm filename
git add file
先从工作区删除文件，然后再暂存删除内容
法二：`git rm filename`
把文件从工作区和暂存区同时删除
法三：`git rm --cached filename`
把文件从暂存区删除，但保留在当前工作区中
注意最后均需 `git commit` 提交

tips: 
==**没太看懂**==
`git rm -r `递归删除某个目录下所有的子目录和文件
- **递归（Recursive）**：在计算机里，它指“自己调用自己”。具体到删除操作，就是 **“如果遇到子文件夹，就先进入这个子文件夹，把它里面的东西删完，再退出来删这个空文件夹”** 的过程。
- 为什么需要递归？
- 普通删除命令只能删除单个文件夹

